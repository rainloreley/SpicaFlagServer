name: Deploy to CapRover

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: CapRover App Deploy
        uses: floms/action-caprover@v1
        with:
          host: 'https://captain.main01.abmgrt.dev'
          password: '${{ secrets.CAPROVER_PASSWORD }}'
          app: 'spicaring'
